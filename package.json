{
  "name": "nightshift",
  "version": "0.1.0",
  "description": "Your agent works the night shift — a live board + flight recorder for AI agent sessions, rendered from an append-only event log.",
  "license": "MIT",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "demo": "node demo/generate.js && node server.js --log demo/events.jsonl",
    "import": "node tools/import-transcript.js",
    "setup": "git config core.hooksPath .githooks && echo 'git hooks enabled (core.hooksPath=.githooks)'"
  }
}
